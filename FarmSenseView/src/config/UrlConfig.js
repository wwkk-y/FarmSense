const baseUrl = "http://localhost:539"

const umsBaseUrl = `${baseUrl}/ums`
const fileBaseUrl = `${baseUrl}/file`
const cmsBaseUrl = `${baseUrl}/cms`
const kaBaseUrl = `ws://localhost:7020` // FIXME
const yolov5BaseUrl = "http://1.15.232.25:699"

export default baseUrl;

export {
    baseUrl,
    umsBaseUrl,
    fileBaseUrl,
    cmsBaseUrl,
    kaBaseUrl,
    yolov5BaseUrl
}